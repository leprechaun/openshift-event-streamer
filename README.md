# openshift-event-streamer

This is just a silly image that pipes openshift / kubernetes events back to Slack (and stdout).

Because when the cluster beyond your control is unreliable, you'll have a bad time.
